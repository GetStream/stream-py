[tool.poetry]
name = "stream-sdks"
version = "0.1.0"
description = ""
authors = ["sachaarbonel <sacha.arbonel@hotmail.fr>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8.16"
httpx = "^0.19.0"
pyjwt = "^2.6.0"

frozendict = "^2.3.7"
click = "^8.1.3"

[tool.poetry.dev-dependencies]
black = "^23.3.0"

[tool.poetry.scripts]
create-token = "stream-sdks.cli:create_token"

[tool.poetry.group.dev.dependencies]
python-dateutil = "^2.8.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
