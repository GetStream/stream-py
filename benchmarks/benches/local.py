"""Local microbenchmarks (filled in a follow-up commit)."""

from __future__ import annotations

from typing import Any


async def run_local_benches(
    *, iterations: int, warmup: int
) -> tuple[list[dict[str, Any]], list[dict[str, str]]]:
    return [], []
